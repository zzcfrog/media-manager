# UE Design — 视频分析器

## 整体风格

- **桌面优先**的 Electron 应用，无移动端适配
- **暗色主题**为默认，支持亮色和跟随系统
- 8 种可选主题色（accent），默认蓝紫 `#6c8cff`
- Quasar 组件库，圆角卡片（16px），毛玻璃对话框（backdrop-filter: blur）
- Lottie 动画用于 AI 分析的等待状态

## 主题系统

三种模式：暗色 → 亮色 → 跟随系统。通过顶部工具栏调色板按钮切换。

| 变量 | 暗色 | 亮色 |
|------|------|------|
| `--bg` | `#0a0a0a` | `#f5f5f5` |
| `--surface` | `#141414` | `#fff` |
| `--text` | `#e8e8e8` | `#222` |
| `--accent` | 用户可选 | 用户可选 |

可选主题色：蓝紫、浅蓝、青色、绿色、琥珀、深橙、红色、紫色。

---

## 页面结构

### 导航

Hash 路由，无 Vue Router：
- `#/gallery` — 媒体库（默认）
- `#/favorites` — 收藏筛选
- `#/collections/:id` — 合集筛选
- `#/detail/:id` — 媒体详情

Gallery 使用 `<keep-alive>` 缓存状态，Detail 每次进入重建。

### 页面布局

```
┌──────────────────────────────────────────────────┐
│  Header（标题 + 主题切换）                         │
├──────────┬───────────────────────────────────────┤
│          │                                       │
│  左侧栏   │              主内容区                   │
│  (260px  │                                       │
│  素材库   │                                       │
│  ├目录树  │                                       │
│  收藏夹   │                                       │
│  标签)   │                                       │
│          │                                       │
├──────────┴───────────────────────────────────────┤
│  Footer（统计信息 + 缩略图大小滑块）                 │
└──────────────────────────────────────────────────┘
```

左侧栏（260px 宽，从上到下）：素材库（可展开，内嵌文件夹目录树）→ 收藏夹列表 → 标签列表，可折叠。

---

## Gallery 页面

### 筛选栏

顶部水平排列，flex-wrap 自动换行：

| 控件 | 说明 |
|------|------|
| 类型切换 | 全部 / 图片 / 视频（`q-btn-group` + 独立 `q-btn`，各带 `q-tooltip` 1s 延迟） |
| 星级筛选 | 1-5 星，点击切换 |
| 颜色标签 | 5 色圆点（红黄绿蓝紫），点击切换 |
| 收藏 | 仅显示收藏 |
| 重置 | 有激活筛选时显示 |
| 搜索框 | 全文搜索（文件名 + 分析内容） |
| 排序 | 下拉选择 + 升降序切换 |
| 分组 | 按时间/时长分组（排序字段为时间时可用） |
| 视图 | 网格 / 列表 切换 |

### 网格视图

CSS Grid 自适应列数，卡片宽度通过底部滑块控制（0.5x ~ 2x）。可按时间线分组显示。

**卡片内容**：
- 16:9 缩略图
- 类型图标（视频/图片）
- 收藏红心
- 颜色圆点
- 星级
- 文件名、分辨率/相机/时长

### 列表视图

表头固定，可排序。每行显示 64x36 缩略图 + 文件名 + 分辨率 + 时长 + 大小 + 拍摄日期 + 导入日期 + 评分/收藏/颜色/分析状态。

### 交互

| 操作 | 行为 |
|------|------|
| 单击 | 选中 |
| Ctrl/Cmd+点击 | 切换选中 |
| Shift+点击 | 范围选中 |
| 双击 | 打开详情 |
| 右键 | 自定义上下文菜单（查看详情、移除） |
| 拖拽框选 | Lasso 选择（自动滚动边缘） |
| Ctrl+Delete | 删除选中 |
| Enter | 打开选中项详情 |

### 导入流程

三步弹窗：

1. **选择**：拖拽区域 + "选择文件" / "选择目录" 按钮（Electron 原生对话框）
2. **进度**：扫描文件 → 3 个并发导入 → 线性进度条 + 已导入缩略图网格
3. **完成**：导入数、跳过数（重复）、失败数 + 可展开详情列表

### 文件夹目录树

嵌套在侧边栏"素材库" `q-expansion-item` 内部，展开后显示 `q-tree` 目录树（`content-inset-level="0.1"` 缩进）。

```
▼ 素材库 (155)
  ├─ 📁 Photos    120
  │   ├─ 📁 2025   80
  │   └─ 📁 2026   40
  └─ 📁 Videos     35
      ├─ 📁 Travel  20
      └─ 📁 Family  15
```

**样式**：
- 无连接线（`no-connectors`），紧凑模式（`dense`）
- 每个节点：文件夹图标（灰色 `folder`）+ 文件夹名 + 右侧媒体计数（灰色小字）
- 默认全部展开（`default-expand-all`）
- 选中节点：背景变 `var(--accent-dim)` + 白色文字
- 长文件夹名 ellipsis 截断 + `q-tooltip` 显示全名
- 无文件夹时显示"暂无文件夹"提示
- 区域可滚动（`overflow-y: auto`），占满剩余空间
- 抽屉宽度 260px（原 220px）

**交互**：

| 操作 | 行为 |
|------|------|
| 点击文件夹 | 选中该文件夹，画廊仅显示此文件夹及子文件夹下的媒体 |
| 再次点击已选中文件夹 | 取消选中，恢复显示全部 |
| 选中文件夹时点击"素材库" | 清除文件夹筛选，回到全部媒体 |
| 切换到收藏/合集视图 | 自动收起素材库目录树（`libraryExpanded=false`） |
| 切回画廊视图 | 自动展开素材库目录树（`libraryExpanded=true`） |

---

## Detail 页面

### 布局

双栏布局：

```
┌──────────────────────────┬─────────────────────┐
│                          │  工具栏（返回、评分、   │
│     媒体查看区域           │  颜色、收藏）          │
│                          ├─────────────────────┤
│  ┌────────────────────┐  │  元数据面板           │
│  │   视频播放器 / 图片   │  │  （技术信息 + 相机     │
│  │                    │  │   信息）              │
│  └────────────────────┘  ├─────────────────────┤
│  ┌────────────────────┐  │                     │
│  │   音频波形           │  │  分析结果            │
│  └────────────────────┘  │  （分段列表 /         │
│  ┌────────────────────┐  │   分析进度）          │
│  │ Waveform │Parade │Vec│  │                     │
│  └────────────────────┘  │                     │
└──────────────────────────┴─────────────────────┘
       flex: 1                     420px 固定
```

### 工具栏

- 返回按钮 → `#/gallery`
- 文件名标题
- 星级评分（1-5 星，可点击）
- 颜色标签（5 色圆点）
- 收藏按钮

### 元数据面板

双列布局，左侧信息带蓝色左边框：

| 左侧 | 右侧 |
|------|------|
| 分辨率、时长、编码、FPS | 制造商、机型、镜头 |
| 色彩空间、码率 | 文件路径（可在 Finder 中显示） |
| 音频编码/采样率/声道 | 文件大小、导入日期、拍摄日期 |

### 视频播放器

原生 HTML5 `<video>` 控件，下方三个 Canvas：

1. **音频波形**（64px 高）：点击跳转，播放时带动画游标
2. **Waveform Scope**：亮度分布
3. **Parade Scope**：RGB 三通道波形
4. **Vectorscope**：色度圆形分布图

播放时以 ~15fps 实时渲染。图片显示 RGB 直方图。

### 设置弹窗

点击顶部工具栏设置图标打开，两个区域，分区标题使用左侧强调条（`border-left: 3px solid var(--accent)`），底部"确定"按钮保存。

**视频分析**：
- 压缩分辨率：480P（推荐）/ 320P / 240P
- 帧率：30fps（推荐）/ 20fps / 10fps
- AI 模型：下拉选择（智谱 GLM 系列等）
- API Key：密码输入框，右侧眼睛图标切换显示/隐藏，留空则使用系统环境变量

**音频分析**：
- 开关："使用视频模型进行多模态解析"（默认开启）
- ASR 引擎选择（仅在开关关闭时可用，当前支持本地 Whisper）
- ASR API Key（仅在开关关闭时显示）：密码输入框 + 眼睛图标，云端 ASR 时填写，本地模型可留空

### AI 分析确认弹窗

点击"AI 分析"按钮时弹出，**只读确认**（不可编辑），显示当前全局设置摘要：

```
┌─────────────────────────────────┐
│  确认分析                         │
│                                 │
│  压缩    480P / 30fps            │
│  模型    智谱 GLM-4.6V           │
│  音频    多模态解析               │
│                                 │
│  ℹ AI 模型分析可能会产生费用       │
│                                 │
│         [取消]  [开始分析]         │
└─────────────────────────────────┘
```

如需修改设置，需前往设置弹窗。

### 分析进度

时间线样式，阶段数根据音频模式动态变化：

**多模态模式（3 阶段）**：
```
 ● 视频压缩  ● 视频编码  ● AI 音视频分析
```

**独立 ASR 模式（4 阶段）**：

```
 ● 视频压缩  ● 视频编码  ● AI 模型分析  ● 语音识别(ASR)
```

- 圆点：等待灰色 → 进行中脉冲动画 + 蓝色 → 完成绿色勾
- 进度条 + 状态文本 + 已用时间
- AI 分析阶段显示打字机效果的随机分析描述（20 条轮播）

### 分析结果

分段列表，每段显示，所有字段可编辑：

```
 ┌───────────────────────────────────────┐
 │ 00:05.20 → 00:12.80  (7.6s)          │
 │ 🖼 一位身穿传统服饰的舞者在...          │  ← 可编辑 (contenteditable)
 │ 💬 对话内容...                         │  ← 可编辑
 │ 📝 字幕内容...                         │  ← 可编辑
 │ [全] [广角] [平视] [固定] [客观]        │  ← 镜头维度（可编辑标签）
 │ [自然] [平静] [自然光] [晴天]           │  ← 场景维度（可编辑标签）
 │ 🔵 白色 ×  🔵 蓝色 ×                  │  ← 颜色（点击 × 移除）
 │ 🏷 舞者 ×  🏷 舞台 ×                  │  ← 主体（点击 × 移除）
 └───────────────────────────────────────┘
```

- 点击分段跳转视频到对应时间
- 播放时高亮当前分段（左边框变主题色）并自动滚动到可见区
- **点击即编辑**：所有文本字段使用 `contenteditable`，点击进入编辑，失焦自动保存
- **标签编辑**：颜色/主体药丸标签有 × 按钮，点击移除并保存
- **编辑态样式**：hover 浅灰背景，focus 主题色边框
- **键盘兼容**：编辑状态时快捷键自动禁用（`isContentEditable` 检测）

### 分析维度颜色编码

| 类别 | 维度 | 颜色 |
|------|------|------|
| 镜头 | 景别 | 绿 `#81c784` |
| 镜头 | 焦段 | 青 `#80deea` |
| 镜头 | 视角 | 粉 `#f48fb1` |
| 镜头 | 运镜 | 浅绿 `#a5d6a7` |
| 镜头 | 透视 | 珊瑚 `#ef9a9a` |
| 场景 | 场景类型 | 蓝 `#64b5f6` |
| 场景 | 情绪 | 紫 `#ce93d8` |
| 场景 | 光线 | 黄 `#fff176` |
| 场景 | 天气 | 橙 `#ffb74d` |

---

## 通用组件

| 组件 | 实现方式 |
|------|---------|
| 通知 | `Quasar.Notify`，顶部弹出，1800ms 自动消失 |
| 对话框 | `q-dialog` + `q-card`，16px 圆角，毛玻璃背景。下拉菜单覆盖方角样式 `.q-menu--square { border-radius: 8px }` |
| 上下文菜单 | 自定义 HTML 定位，含 `q-list` + 快捷键提示 |
| 颜色选择 | 行内圆点，点击切换（非独立组件） |
| 进度条 | `q-linear-progress` |
| 动画 | Lottie（AI 分析等待） |
| 无限滚动 | IntersectionObserver，200px 预加载距离 |
